Client:
  FlyerProperties:
    type: object
    required:
      - showFlyer
      - body
    properties:
      showFlyer:
        type: boolean
      body:
        type: string
      flyerType:
        type: string

  Theme:
    type: string
    enum:
      - default
      - red
      - green
      - blue
